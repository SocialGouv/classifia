meta {
  name: new conversation
  type: http
  seq: 1
}

post {
  url: {{base_url}}/conversations
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  conversation_id: {{conversation_id}}
}

settings {
  encodeUrl: true
}
